export type QuoteLine = {
  id: string;
  quote_id: string;
  product_id: string;
  quantity: number;
  unit_price: number;
  line_total: number;
};

export type QuoteLineRow = {
  id: string;
  quote_id: string;
  product_id: string;
  quantity: number;
  unit_price: number;
  line_total: number;
};
